function minhaFunc(param) {
    //bloco de codigo
}

minhaFunc("param")

// Expressão de função 

const soma = function(num1, num2) {return num1 + num2}
console.log(soma(17, 28))

// Diferenças principal ????

console.log(apresentar())

function apresentar() {
    return "Olá";
}

console.log(soma1(1, 2)) // se a função não é pre declarada acontecera erros! | ReferenceError: Cannot access 'soma1' before initialization
const soma1 = function(num1, num2) {return num1 + num2}
